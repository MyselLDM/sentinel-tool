# Normalize line endings so scripts run correctly on every platform.

# Shell scripts must keep LF endings — CRLF breaks `bash` on Windows checkouts.
*.sh text eol=lf

# PowerShell scripts use CRLF (Windows convention).
*.ps1 text eol=crlf
