"""Sentinel inference service (FastAPI).

Loads the two fine-tuned MiniLM models (NLI cross-encoder + contrastive
bi-encoder) and exposes them over HTTP. See ``fastapi/README.md`` for the API.
"""

__version__ = "0.1.0"
